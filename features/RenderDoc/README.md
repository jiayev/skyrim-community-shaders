RenderDoc integration feature

Place renderdoc.dll and any helper runtime DLLs into this folder before packaging. This folder is treated as a core feature and will be included into the AIO package.

When installing to the game Data directory, the RenderDoc DLLs should end up in Renderdoc/ for runtime loading by the plugin.
